# loyalty-service
 **loyalty-service** manages user loyalty points and tiers.  ## Endpoints - `GET /health` — service status - `POST /loyalty/add-points` — add points to a user (sample) - `GET /loyalty/:userId` — get user loyalty info (sample) - `POST /loyalty/redeem` — redeem points for a user (sample)  ## Tracing This service reports telemetry
